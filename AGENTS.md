> This project is a TanStack Start (React) + Nitro app deployed on Vercel.
> The backend is an embedded Express server (`server/`), bridged into the app
> via srvx `fetchNodeHandler` in `src/server.ts`.
>
> Build: `npm run build` (Vite + Nitro `vercel` preset, output in `.vercel/output`).